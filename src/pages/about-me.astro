---
import { getEntryBySlug } from "astro:content";
import OriginalComponent from "./[slug].astro";
import Mermaid from "../components/Mermaid.astro";

const page = await getEntryBySlug("pages", "about-me");
---

<OriginalComponent {...page} />
<Mermaid />
